using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cricket : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
       //Output to the console crickets
       print("Crickets"); 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
